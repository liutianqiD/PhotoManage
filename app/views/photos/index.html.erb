<p style="color: green"><%= notice %></p>

<h1>写真一覧</h1>
<%= link_to "写真アップロード", new_photo_path, method: "get" %>
<div id="photos">
  <% @photos.each do |photo| %>
  <%= render photo %>
  <% end %>
</div>
